#include<bits/stdc++.h>
#include<iostream>
using namespace std;
int main(){
	int n,sum=0;
	cin>>n;
	long int A[n];
	for(int i=0;i<n;i++){
		cin>>A[i];
        sum=A[i]/10;
	}
	if(sum%10==0){
		cout<<"Yes";
	}
	else{
		cout<<"No";
	}
	return 0;
}
